#include <iostream>
#include <vector>
#include <optional>

using namespace std;


template<typename Key, typename Value>
class OpenAddressHashMap {
private:
    struct

    // core fields: Entry[], size, capacity, etc.
    // insert(key, value)
    // get(key)
    // contains(key)
    // rehash(), etc.
};